%h1 Moje pozycje
%p Tutaj znajduje się lista książek, które posiadasz
%ul
   - @books.each do |b|
      %li= link_to b.book.title, :action => "show", :id => b.book.id
%br
%br
%p= link_to "Wróć na główną", :controller => "books"
