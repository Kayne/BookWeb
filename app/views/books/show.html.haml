%h1= @book.title
%p
   = image_tag @book.photo.url(:medium)
%p
   = @book.desc
   %br
   %i= @book.author
%p Książkę posiadają:
%ul
   - @assigments.each do |a|
      %li= link_to a.user.email, :controller => "/profiles", :action => "show", :id => a.user.id
%br
%br
= link_to "Edytuj pozycję", edit_book_path
%br
= link_to "Powrót na główną", {:action => "index"}
