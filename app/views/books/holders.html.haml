%div{:class => "holders"}
   - if @assigments.size > 0 then
      %p 
         Książkę posiadają:
         %ul
         - @assigments.each do |a|
            %li= link_to a.user.username, :controller => "/profiles", :action => "show", :id => a.user.username
   - else
      %p Nikt nie posiada tej książki.
