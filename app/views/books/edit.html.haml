%h1 Edytuj książkę
%p
   Możesz użyć poniższego formularza by edytować dodaną już książkę.
   = form_for(@book, :html => { :multipart => true }) do |f|
      %p
         %h2= @book.title
      %p{:class => "form"}
         = f.label :desc, "Opis książki"
         = f.text_area :desc, :value => @book.desc
      %p{:class => "form"}
         = f.label :author, "Autor książki"
         = f.text_field :author, :value => @book.author
      %p{:class => "form"}
         = f.label :photo, "Zdjęcie"
         = f.file_field :photo, :value => @book.photo.url
         %i (Zdjęcie nie jest obowiązkowe!)
      %p{:class => "form"}
         = f.submit "Edytuj pozycję"
%div{:style => "clear: both;"}
%p= link_to 'Wróć', book_path(@book)
